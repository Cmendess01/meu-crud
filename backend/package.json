{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"  // Comando para iniciar o servidor
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",  // Biblioteca para processar dados do corpo das requisições
    "cors": "^2.8.5",         // Biblioteca para permitir requisições de diferentes origens
    "express": "^5.1.0",      // Framework web para Node.js
    "pg": "^8.16.3"           // Cliente PostgreSQL para Node.js
  },
  "description": "Backend do sistema CRUD de clientes"
}
